spring:
  datasource:
    url: jdbc:mysql://localhost:3306/firstapp9?useSSL=false
    username: root
    password: 4444
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
